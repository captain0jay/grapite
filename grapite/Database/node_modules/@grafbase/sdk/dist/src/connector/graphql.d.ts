import { Header, HeaderGenerator } from './header';
import { SchemaTransform, TransformsGenerator } from './transforms';
export interface GraphQLParams {
    url: string;
    headers?: HeaderGenerator;
    transforms?: TransformsGenerator;
}
export declare class PartialGraphQLAPI {
    private apiUrl;
    private headers;
    private introspectionHeaders;
    private transforms;
    constructor(params: GraphQLParams);
    finalize(namespace?: string): GraphQLAPI;
}
export declare class GraphQLAPI {
    private namespace?;
    private url;
    private headers;
    private introspectionHeaders;
    private transforms;
    constructor(url: string, headers: Header[], introspectionHeaders: Header[], transforms: SchemaTransform[], namespace?: string);
    toString(): string;
}
//# sourceMappingURL=graphql.d.ts.map