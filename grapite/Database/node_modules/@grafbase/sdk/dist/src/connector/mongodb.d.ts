import { ModelFields, MongoDBModel } from './mongodb/model';
export interface MongoDBParams {
    name: string;
    apiKey: string;
    appId: string;
    dataSource: string;
    database: string;
}
export declare class PartialMongoDBAPI {
    private name;
    private apiKey;
    private appId;
    private dataSource;
    private database;
    private models;
    constructor(params: MongoDBParams);
    /**
     * Creates a new model type with an access to this MongoDB data source.
     *
     * @param name - The name of the model
     * @param fields - The fields of the model
     */
    model(name: string, fields: ModelFields): MongoDBModel;
    finalize(namespace?: string): MongoDBAPI;
}
export declare class MongoDBAPI {
    private name;
    private apiKey;
    private appId;
    private dataSource;
    private database;
    private namespace?;
    models: MongoDBModel[];
    constructor(name: string, apiKey: string, appId: string, dataSource: string, database: string, models: MongoDBModel[], namespace?: string);
    toString(): string;
}
//# sourceMappingURL=mongodb.d.ts.map